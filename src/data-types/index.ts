export interface LoginForm {
  username: string;
  password: string;
}

export interface Attendance {
  // createdAt: string;
  name: string;
  position: string;
  institution: string;
  purpose: string;
}
